# MiniMax Provider Adapter

Use for campaign variants, social clips and character motion.

Secrets only in local `.env`:

```env
MINIMAX_SUBSCRIPTION_KEY=
MINIMAX_API_KEY=
MINIMAX_GROUP_ID=
MINIMAX_BASE_URL=https://api.minimax.io
```

Never commit, log, document or package real keys.
